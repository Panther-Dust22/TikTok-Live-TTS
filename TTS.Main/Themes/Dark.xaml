<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <!-- Dark Theme Colors -->
    <SolidColorBrush x:Key="AppBackgroundBrush" Color="#1E1E1E"/>
    <SolidColorBrush x:Key="AppHeaderBackgroundBrush" Color="#2E3440"/>
    <SolidColorBrush x:Key="AppTextBrush" Color="#FFFFFF"/>
    <SolidColorBrush x:Key="AppSecondaryTextBrush" Color="#D8DEE9"/>
    <SolidColorBrush x:Key="AppBorderBrush" Color="#4C566A"/>
    <SolidColorBrush x:Key="AppButtonAreaBackgroundBrush" Color="#1E1E1E"/>
    <SolidColorBrush x:Key="AppSwitchBackgroundBrush" Color="#4C566A"/>
    <SolidColorBrush x:Key="AppConsoleBackgroundBrush" Color="#1E1E1E"/>
    <SolidColorBrush x:Key="AppConsoleTextBrush" Color="#FFFFFF"/>
    <SolidColorBrush x:Key="AppQueueBackgroundBrush" Color="#1E1E1E"/>
    <SolidColorBrush x:Key="AppQueueTextBrush" Color="#FFFFFF"/>
    <SolidColorBrush x:Key="NameSwapBorderBackgroundBrush" Color="#FFFFFF"/>
    
    <!-- Apply to Window -->
    <Style TargetType="Window">
        <Setter Property="Background" Value="{DynamicResource AppBackgroundBrush}"/>
    </Style>
    
        <!-- Apply to TextBlocks -->
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource AppTextBrush}"/>
            <Setter Property="TextOptions.TextFormattingMode" Value="Display"/>
            <Setter Property="TextOptions.TextRenderingMode" Value="ClearType"/>
            <Setter Property="RenderOptions.BitmapScalingMode" Value="HighQuality"/>
            <Setter Property="RenderOptions.EdgeMode" Value="Aliased"/>
        </Style>
    
    <!-- Apply to Buttons (except functional ones) -->
    <Style TargetType="Button">
        <Setter Property="Background" Value="{DynamicResource AppButtonAreaBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{DynamicResource AppTextBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource AppBorderBrush}"/>
    </Style>
    
    <!-- Apply to TextBoxes -->
    <Style TargetType="TextBox">
        <Setter Property="Background" Value="{DynamicResource AppButtonAreaBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{DynamicResource AppTextBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource AppBorderBrush}"/>
    </Style>
    
    <!-- Apply to ComboBoxes -->
    <Style TargetType="ComboBox">
        <Setter Property="Background" Value="{DynamicResource AppButtonAreaBackgroundBrush}"/>
        <Setter Property="Foreground" Value="Black"/>
        <Setter Property="BorderBrush" Value="{DynamicResource AppBorderBrush}"/>
    </Style>
    
    <!-- Apply to ComboBoxItems -->
    <Style TargetType="ComboBoxItem">
        <Setter Property="Foreground" Value="Black"/>
        <Setter Property="Background" Value="White"/>
    </Style>
    
    <!-- Apply to ComboBox TextBox (editable part) -->
    <Style TargetType="ComboBox" x:Key="ComboBoxEditableTextBoxStyle">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Grid>
                        <ToggleButton Name="ToggleButton" Grid.Column="2" Focusable="false" IsChecked="{Binding Path=IsDropDownOpen,Mode=TwoWay,RelativeSource={RelativeSource TemplatedParent}}" ClickMode="Press">
                            <Path Name="Arrow" Fill="Black" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,5,0" Data="M 0 0 L 4 4 L 8 0 Z"/>
                        </ToggleButton>
                        <ContentPresenter Name="ContentSite" IsHitTestVisible="False" Content="{TemplateBinding SelectionBoxItem}" ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}" Margin="3,3,23,3" VerticalAlignment="Center" HorizontalAlignment="Left"/>
                        <TextBox x:Name="PART_EditableTextBox" IsReadOnly="{TemplateBinding IsReadOnly}" Background="{TemplateBinding Background}" Foreground="Black" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="3,3,23,3" Focusable="True" />
                        <Popup Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                            <Grid Name="DropDown" SnapsToDevicePixels="True" MinWidth="{TemplateBinding ActualWidth}" MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                <Border Name="DropDownBorder" Background="{TemplateBinding Background}" BorderThickness="{TemplateBinding BorderThickness}" BorderBrush="{TemplateBinding BorderBrush}"/>
                                <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                    <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                </ScrollViewer>
                            </Grid>
                        </Popup>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
    <!-- Apply to Borders -->
    <Style TargetType="Border">
        <Setter Property="BorderBrush" Value="{DynamicResource AppBorderBrush}"/>
    </Style>
    
    <!-- Apply to ScrollViewer -->
    <Style TargetType="ScrollViewer">
        <Setter Property="Background" Value="{DynamicResource AppConsoleBackgroundBrush}"/>
    </Style>
    
    <!-- Apply to ListBox -->
    <Style TargetType="ListBox">
        <Setter Property="Background" Value="{DynamicResource AppConsoleBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{DynamicResource AppConsoleTextBrush}"/>
    </Style>
    
    <!-- Specific style for NameSwapTextBox -->
    <Style x:Key="NameSwapTextBoxStyle" TargetType="TextBox">
        <Setter Property="Background" Value="White"/>
        <Setter Property="Foreground" Value="Black"/>
        <Setter Property="BorderBrush" Value="{DynamicResource AppBorderBrush}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            SnapsToDevicePixels="True">
                        <ScrollViewer x:Name="PART_ContentHost" Focusable="False" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
</ResourceDictionary>
